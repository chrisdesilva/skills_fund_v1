import React from "react"
import Layout from "../components/layout/Layout"

export default () => {
  return (
    <Layout>
      <p>Whoops. Looks like there isn't a page here.</p>
    </Layout>
  )
}
